# 🚀 Installation Laravel - Site Lenad Consulting

## ⚡ SOLUTION LA PLUS RAPIDE (SANS Laravel)

**Pour voir le site MAINTENANT sans installation :**

👉 **Double-cliquez sur** : `index.html`

Le site s'ouvrira directement dans votre navigateur !

---

## 🔧 Installation Laravel (Version Complète)

### Prérequis

1. **PHP 8.1+** : https://www.php.net/downloads.php
2. **Composer** : https://getcomposer.org/download/

### Installation Rapide

#### Étape 1 : Installer Composer

1. Allez sur : https://getcomposer.org/download/
2. Téléchargez `Composer-Setup.exe`
3. Installez en suivant les instructions
4. Vérifiez : `composer --version`

#### Étape 2 : Installer Laravel

Ouvrez PowerShell dans le dossier `laravel` :

```powershell
cd laravel
composer install
```

#### Étape 3 : Configuration

```powershell
copy .env.example .env
php artisan key:generate
```

#### Étape 4 : Lancer le Serveur

```powershell
php artisan serve
```

#### Étape 5 : Ouvrir dans le Navigateur

Ouvrez : **http://localhost:8000**

---

## 📋 Commandes Complètes

```powershell
# 1. Aller dans le dossier laravel
cd laravel

# 2. Installer les dépendances
composer install

# 3. Créer le fichier .env
copy .env.example .env

# 4. Générer la clé
php artisan key:generate

# 5. Lancer le serveur
php artisan serve

# 6. Ouvrir http://localhost:8000
```

---

## 🎯 Alternative : Version HTML Simple

Si vous voulez voir le site **IMMÉDIATEMENT** sans installation :

**Double-cliquez sur** : `index.html`

Tout fonctionne sans installation !

---

## 📁 Structure Laravel

```
laravel/
├── app/
│   └── Http/Controllers/
├── resources/
│   └── views/
│       ├── layouts/
│       └── sections/
├── routes/
│   └── web.php
├── public/
│   ├── css/
│   ├── js/
│   └── images/
└── composer.json
```

---

## ✨ Fonctionnalités Laravel

- ✅ Routes et contrôleurs
- ✅ Vues Blade modulaires
- ✅ Validation de formulaire Laravel
- ✅ Sessions pour multilingue
- ✅ Structure professionnelle
- ✅ Toutes les animations GSAP incluses

---

**Pour voir le site RAPIDEMENT : Double-cliquez sur `index.html` ! 🚀**

